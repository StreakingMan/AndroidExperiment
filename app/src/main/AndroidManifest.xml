<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.maxstudio.androidexperiment">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="安卓移动开发实验">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".HelloWorldActivity"
            android:label="Hello World" />
        <activity
            android:name=".AddCommodityActivity"
            android:label="添加商品" />
        <activity
            android:name=".CommodityDetailActivity"
            android:label="商品详情" />
        <activity
            android:name=".ManagementActivity"
            android:label="商品管理页面" />
        <activity android:name=".ShowCommodityListActivity"
            android:label="商品列表页"></activity>
    </application>

</manifest>